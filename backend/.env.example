# Django Settings
DJANGO_SECRET_KEY=
DEBUG=True

# Database (PostgreSQL example, but defaults to SQLite if not set)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=

# Google Cloud & Gemini AI Keys
GEMINI_API_KEY=
# IMPORTANT: For Google Cloud Vision, you must set up Application Default Credentials.
# 1. Download your service account JSON key file from Google Cloud.
# 2. Set this environment variable to the absolute path of that file.
GOOGLE_CLOUD_VISION_API_KEY=

# Email Settings (use an App Password for Gmail)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
AUTHORITY_EMAIL=
NGO_EMAIL=
